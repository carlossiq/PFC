Use the existing project structure and implement only this module.
Do not modify unrelated files.

Act as a senior Python backend engineer and implement the search services layer for a FastAPI technology prospecting API.

Requirements:
- code in English
- function comments in Portuguese
- full typing
- no pseudocode

Implement:
- services/search/lens_service.py
- services/search/ops_service.py
- services/search/scopus_service.py

Rules:
- use httpx
- use config-driven URLs and credentials
- implement retry logic
- log retries and failures with run_id
- continue execution if one API fails
- return structured error information

Probe search:
- use PROBE_API as required
- use PROBE_API_EXT as optional complementary source
- support patent, scholarly, or both

General search:
- execute enabled APIs in this order:
  1. Lens scholarly
  2. Lens patent
  3. OPS
  4. Scopus

OPS special case:
- implement OAuth2 client credentials token retrieval
- store access_token, expires_in, expiration timestamp
- auto-refresh token when needed
- make explicit TODOs for complementary metadata retrieval stages

Scopus:
- implement pagination
- continue next pages only if results still look potentially relevant

Return:
- full code for all files
- response models or typed structures
- clean error handling